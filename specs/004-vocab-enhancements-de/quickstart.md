# Quickstart

## Prerequisites
- Node.js 18+
- OpenAI API Key (in `.env.local`)

## Setup
1. `npm install`
2. `cp .env.example .env.local` (and add `OPENAI_API_KEY`)

## Running the App
```bash
npm run dev
```
Visit `http://localhost:3000`.

## Testing
```bash
# Unit tests (Vitest)
npm test
```
