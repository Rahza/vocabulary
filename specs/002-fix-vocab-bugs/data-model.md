# Data Model

No changes to the persistent data model (`VocabularyPair`, `LeitnerState`, etc.).

**Runtime Entities (Transient)**:

- **Toast**: Managed by `sonner` library (message, type, duration).
